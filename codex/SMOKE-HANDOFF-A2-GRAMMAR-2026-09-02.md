# Handoff — A2 grammar smoke (2026-09-02, end of hour)

**Superseded 2026-09-03.** New tab: `codex/SMOKE-HANDOFF-A2-GRAMMAR-2026-09-03.md`.

Give this file to a **new tab**. Do **not** open `codex/HANDOFF.md` (Claude kill switch). Do not continue the long thread.

Paste:

```
Read codex/SMOKE-HANDOFF-A2-GRAMMAR-2026-09-02.md then AGENTS.md · AUTHORING-RULES.md (I8 after flags · I6 after tick · C1 · C11 · C14 · C18 · C53 · C54 · C55 · E7).
Rue-exp. Port 8097. Progress key rue-exp-progress. Codex: ../rue-codex.
Do not invent node ids. Do not tick INSPECTED by hand. Do not start A2 vocab (separate rail / separate tab).
Job: A2 grammar smoke from the path. Next play is in this file. Hour ended 2026-09-02: intros were the drag — remaining card 0s are listed below. Dropdowns before any rewrite. Ctrl+F5 after edits.
```

---

## Product

| | |
|--|--|
| Folder | `C:\Users\ADMIN\documents\projects\rue-exp` |
| Port | 8097 |
| Progress | `rue-exp-progress` only |
| Bar | usable, not perfect |
| Scope | **A2 grammar only.** Skip `a2_grammar_match` / `a2_grammar_type` / `a2_finale` (engine still A1). Vocab leaves are on the Telegram zigzag — **this tab does not play them.** |

## Do not

- Invent node ids · hand-tick INSPECTED · touch `codex/HANDOFF.md`
- Start B1 vocab extension · refill B2/C1 stubs · push / Pages
- Batch-rewrite intros of smoked units unless James flags
- Type `as_simple_vs_continuous` — the id is **`a2_`** (A-two)

---

## Now

**Next grammar:** http://localhost:8097/#a2_reflexives

Then path order, skipping already-ticked grammar:

`indirect_questions`

Telegram Top 5 is the **A2 zigzag**. For this tab, ignore leaf_* and play the next **a2_** grammar hash. Tick: `a2_reflexives tested`.

Tree label is **a / the / nothing** (not *Articles at A2*). If Telegram still says the old title, the snapshot is stale — `units to test` after rebuild.

Ctrl+F5. Cache-bust if the intro looks stale.

**Before play:** intros pre-fixed. `articles` title **a / the / nothing**. Leave indefinite / say_tell / reflexives unless flagged.

Vocab zigzag is a **different tab**. `leaf_personality_a2` ticked here by mix-up — C56 diagram page already cut. Do not play `leaf_home_a2` in this tab.

---

## Hour-end lesson (2026-09-02)

Drills were fine. **Intros** burned the clock.

| What happened | Rule |
|---------------|------|
| `have_to` card 0 = *outside obligation* only. James: semi-modal, part of the modal set, like must. | **C55** |
| Sort tested *must* vs *have to* and intro never named that pair. Sort itself stayed. | **C11** (now includes sort) |
| `could_able` card 0 = *could = past of can*. James: these are modal verbs; recap the set; link the modals table. | **C55** confirmed |
| `arrangements` LEARNED with no rewrite. Diary vs timetable was enough. | — |

Smoother next hour: **card 0 pre-flight** for C14 title, C55 set, C11 sort contrast, `text_first`. Do not I10-only when the unit joins a class already taught (modals, articles, some/any). Mid-play rewrite + Ctrl+F5 is the expensive path.

---

## Remaining intros

| id | Card 0 now | Status |
|----|------------|--------|
| `a2_too_enough` | **Too / enough**. `need_scale`. Place table card 2. | LEARNED. Quiz = chunk (*big enough* / *too tired*), Czech clue, no `(old / enough)` on the stem. Use `wrong` keeps this form (E11). |
| `a2_articles` | **a / the / nothing**. `text_first`. Pack/node/spine label match. | Pre-fixed C14/C23. |
| `a2_indefinite_pronouns` | Unit name. Stand-alone vs *some/any*. Person/thing matches sort. | Leave. |
| `a2_say_tell` | Four verbs on one page. | C52 split only if he flags. |
| `a2_reflexives` | Same-person IS. No I/you/he *myself…* list. | C54 forms table only if he flags. |
| `a2_indirect_questions` | **Indirect questions**. `text_first`. Direct then after *know*. | Pre-fixed C18/C14. |

---

## Ticked this sitting (2026-09-02)

| id | Notes |
|----|--------|
| `a2_possessive_pronouns` | LEARNED. C53 / C54. Title **Mine / yours / his / hers**. Table I/you/he/she/we/they. |
| `a2_past_questions` | LEARNED. Quality ok, **bank thin** (more questions later if asked). Card 0 = unit name, not *did carries the past*. |
| `a2_simple_vs_continuous` | Telegram (after a typo `as_` — bot logged the wrong id; laptop corrected). |
| `a2_for_since` | LEARNED. Path stays **after present perfect**. Timeline on card 0. Slogan card cut. Quiz chips **for / since** only. Use = error-correction, PP already on the page, only for/since is wrong. |
| `a2_arrangements` | LEARNED. No rewrite. Toolbar EN was the tree screen, not a content flag. No new class. |
| `a2_have_to` | LEARNED. C55 card 0 = modal set (like must), not only *outside*. C11 sort contrast got its own card (*must* = I want this · *have to* = a rule). Chips bold (C37). Four job bins kept. |
| `a2_could_able` | LEARNED. C55: card 0 names modal verbs, recaps the set, links Full table. Title = unit name. |
| `a2_too_enough` | LEARNED. C43 scale. Quiz gaps the chunk (*big enough* / *too tired*); Czech is the clue, no `(old / enough)` on the stem. Use `wrong` keeps this form (**E11**): *too much late*, not *enough late*. |
| `leaf_personality_a2` | Vocab, mixed into this tab. LEARNED. C56: diagram page *What they are like* cut. |
| `a2_articles` | LEARNED. Good little unit, **bank thin** — added 5 (cats / books / water / work / the sky). Logic tree `articles_map`. Sort = situation chips, not *a dog*. Coffee out (*a coffee* = a cup). Type: Enter = no article. Slogan title cut. |
| `a2_indefinite_pronouns` | LEARNED. Map = some/any/no polarity (not person vs thing). Sort = positive / question-negative / zero. Flags: -one/-body both; questions accept someone; stems forced person (*answered the phone*, *came to the party*). |
| `a2_say_tell` | LEARNED. C52 two pair pages. Sort = say/tell/speak only (*speak to* = *talk to*). *talk about* / *speak about* both. **A13** *said that she was*. Intro now/past. Stems lock tense (*yesterday* / *every day*). |

Older A2 grammar (present continuous, agreement, past simple, past continuous, used to, present perfect, will/going to, modals, comparatives, -ed/-ing, countable, quantifiers, some/any, adverbs, first conditional, verb patterns, movement) was already `[x]` from earlier smokes. **Replay only if James asks.**

---

## New rules (observed)

| ID | Rule | From |
|----|------|------|
| **C53** | Form-change card 0 does not define the new form by a job the old form also has. *These words say whose* is also *my / your / his*. Show **with a noun** vs **alone** (C42). | possessives |
| **C54** | Person lists are **I / you / he / she / we / they**. If the title has *hers*, it has *his*. Do not park *his* last because the form does not change. | possessives |
| **C55** | A new member of a closed class already taught opens on **set membership**, not only a cousin contrast. Recap the set; link the full table. | have_to · could_able |
| **E11** | Use `wrong` on a two-form contrast still names this form (or its calque). Not the cousin's error (*enough late* for *too late*). | too_enough |

Also live this sitting (not new IDs): C14 card 0 = unit name; C18 no slogan titles (*Not from · not nothing* cut); B6 Quiz stays on-axis (for/since, not *at/from*); E7 Use error-correction, one error; **C11** now includes sort (must vs have to).

**C56** (vocab rail, 2026-09-02): diagram-only intro pages are grammar. Cut on `a2_personality` (kind vs rude) and remaining A2 vocab (`describing`, `adverbs`, `ideas`, `ideas_2`, `verbs`). Do not replay smoked A1 schematic pages unless James asks. This grammar tab does not play vocab.

---

## Check / intro work already in the packs (do not redo)

- **`text_first: true`** on C1-fail card 0s rewritten this sitting (definition above the table).
- Form packs **Check = Quiz only**, authored chips: `comparatives` (includes *more better*) · `modals_must_should` · `first_conditional` · `prepositions_movement` · `quantifiers`.
- **`a2_have_to` sort bins are jobs:** I want this · a rule · not necessary · don't do it. `sort_cz: false` on have_to, indirect_questions, simple_vs_continuous, arrangements, could_able.
- Tense timelines: keep diagram-first unless James asks (C29). Case by case.

---

## Telegram

Laptop listener: `TelegramSweepBot` on this machine. Ranking: `TA/smoke-order.json` (A2 circle only, hide ticked). Rebuild: `py -X utf8 codex\build_smoke_next.py --write`. After a tick: `py -X utf8 codex\reconcile_inspected.py`.

If the phone still lists A1 vocab, the snapshot is stale — rebuild, then `units to test`. Id is `a2_…` not `as_…`.

Do not hand-edit `INSPECTED.md`.

---

## After a flag

I8: findings → **dropdowns** → rewrite. Not a drive-by. After Telegram `<id> tested`: new class → AUTHORING-RULES `observed` → reconcile → **new tab**.
