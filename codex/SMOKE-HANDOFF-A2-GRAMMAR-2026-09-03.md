# Handoff — A2 grammar smoke (2026-09-03)

Give this file to a **new tab**. Do **not** open `codex/HANDOFF.md` (Claude kill switch). Do not continue the long say_tell / reflexives thread.

Paste:

```
Read codex/SMOKE-HANDOFF-A2-GRAMMAR-2026-09-03.md then AGENTS.md · AUTHORING-RULES.md (I10 prep · I8 after flags · I6 after tick · C1 · C11 · C14 · C18 · C52 · C54 · C55 · A13 · E7 · E11 · F3).
Rue-exp. Port 8097. Progress key rue-exp-progress. Codex: ../rue-codex.
Do not invent node ids. Do not tick INSPECTED by hand. Do not start A2 vocab or B1 (separate tabs). Do not run scripts/sync_from_stable.py.
Job: A2 grammar smoke from the path. Next play is in this file.
Dropdowns before any rewrite. Ctrl+F5 after edits. Skip Match.
```

Older sitting notes: `codex/SMOKE-HANDOFF-A2-GRAMMAR-2026-09-02.md` (superseded). Vocab zigzag: `codex/SMOKE-HANDOFF-A2-VOCAB-2026-09-02.md`. B1: `codex/SMOKE-HANDOFF-B1-2026-09-03.md`.

---

## Product

| | |
|--|--|
| Folder | `C:\Users\ADMIN\documents\projects\rue-exp` |
| Port | 8097 |
| Progress | `rue-exp-progress` only |
| Bar | usable, not perfect |
| Scope | **A2 grammar only.** Skip `a2_grammar_match` / `a2_grammar_type` / `a2_finale` (engine still A1). Telegram Top 5 is the A2 zigzag — **this tab ignores `leaf_*`.** |

## Do not

- Invent node ids · hand-tick INSPECTED · touch `codex/HANDOFF.md`
- Start B1 vocab extension · refill B2/C1 stubs · push / Pages
- Run `scripts/sync_from_stable.py` (it has cut `path_order_b1`)
- Batch-rewrite intros of smoked units unless James flags
- Re-smoke `leaf_health_a1` because *cut* was added (already `[x]`)
- Type `as_simple_vs_continuous` — the id is **`a2_`** (A-two)
- Commit junk `--before` / `--before.json` / `--help` in the repo root (`make_pool` without `POOL.md`)

---

## Now

**In play (not ticked):** http://localhost:8097/#a2_reflexives

Do **not** re-prep unless he asks. Smoke-prep already ran this sitting. Wait for flags or Telegram `a2_reflexives tested`.

Then path order, last remaining A2 grammar:

`indirect_questions` → http://localhost:8097/#a2_indirect_questions

Then **this rail is done.** Stop. Do not start vocab or B1 from this tab.

Ctrl+F5. Cache-bust if the intro looks stale.

---

## Reflexives — already done this sitting (do not redo)

Lint F3: Use-leads *enjoyed / feels / happened / cut* (4 of 8). James: those verbs are pretty essential — fold or teach here. Diagnosis:

| lemma | Where it lives | Action |
|-------|----------------|--------|
| enjoy / feel | A1 Verbs·more (`I enjoy my work` / `I feel tired`) | stay; lint is inflection not lemma |
| happen | A1 Verbs·more 3 (`What happens next?`) | stay; same |
| cut | **never taught** | folded into **A1 Health** next to *hurt* |

Health now has item `cut` / *říznout se*, 🩹 on the picture board, sentence *I cut my finger.* Reflexives Need-self row: **hurt himself · cut herself**. Health already smoked — **no re-tick**.

Use is error-correction (`use_mode: correct`). James chose **play anyway**. If he flags F3 on Use, dropdown before folding more verbs into A1.

C54 myself-list: **leave unless he flags.** A2 is the interference core (*feel good*, *happen to me*). *by myself* / emphasis is a B1 stub — do not expand.

`sort_cz` is not set (chips still have Czech). Leave unless overflow.

---

## Remaining after this play

| id | Card 0 now | Status |
|----|------------|--------|
| `a2_reflexives` | **Reflexives & -self**. Same-person IS. Need-self includes *cut herself*. No I/you/he *myself…* list. | **in play** |
| `a2_indirect_questions` | **Indirect questions**. `text_first`. Direct then after *know*. Sort correct / needs repair. `sort_cz: false`. | unseen. I10 only when he asks or after the reflexives tick. |

---

## Ticked this sitting (2026-09-02 → 03)

| id | Notes |
|----|--------|
| `a2_possessive_pronouns` | LEARNED. C53 / C54. Title **Mine / yours / his / hers**. |
| `a2_past_questions` | LEARNED. Bank thin. Card 0 = unit name. |
| `a2_simple_vs_continuous` | Telegram (typo `as_` once — id is `a2_`). |
| `a2_for_since` | LEARNED. After present perfect. Quiz chips **for / since** only. |
| `a2_arrangements` | LEARNED. No rewrite. Toolbar EN was the tree, not a content flag. |
| `a2_have_to` | LEARNED. **C55** card 0 = modal set (like must). **C11** sort contrast got its own card. |
| `a2_could_able` | LEARNED. C55 confirmed. Title = unit name. Link Full table. |
| `a2_too_enough` | LEARNED. C43 scale. Quiz gaps the chunk. Use **E11** (*too much late*, not *enough late*). |
| `leaf_personality_a2` | Vocab, mixed into the grammar tab. LEARNED. **C56** diagram page cut. Do not play more `leaf_*` here. |
| `a2_articles` | LEARNED. “Good little unit, a bit thin” — +5. `articles_map`. Sort = situation chips. Type: Enter = no article. |
| `a2_indefinite_pronouns` | LEARNED. Map = polarity (someone/something vs anybody/anything vs nobody/nothing). 9 P0 flags applied. |
| `a2_say_tell` | LEARNED. “okay, that’ll do.” **C52** two pair pages (say/tell vs speak/talk) after he flagged the four-row table. Sort = say/tell/speak only (`speak to` = `talk to`). *talk about* / *speak about* both (`Yesterday we ___ about the film`, `gap_accepts` talked/spoke). **A13** *said that she was*. Intro now/past. Stems lock tense (*yesterday* / *every day*). `sort_cz: false`. |

Older A2 grammar (present continuous, agreement, past simple, past continuous, used to, present perfect, will/going to, modals, comparatives, -ed/-ing, countable, quantifiers, some/any, adverbs, first conditional, verb patterns, movement) was already `[x]`. **Replay only if James asks.**

INSPECTED: grammar **66 inspected / 13 unseen**. After reflexives + indirect_questions, remaining A2 grammar is only match / type / finale — skip those.

---

## New rules (keep)

| ID | Rule | From |
|----|------|------|
| **C53** | Form-change card 0 is with vs without, not a job the old form also has. | possessives |
| **C54** | Person lists are **I / you / he / she / we / they**. *hers* in the title means *his* is there. | possessives |
| **C55** | New member of a closed class already taught opens on **set membership**. Recap; link the table. | have_to · could_able |
| **C56** | Diagram-only intro pages are **grammar**. Vocab = pictures then frames. | personality (vocab tab owns the rest) |
| **E11** | Use `wrong` on a two-form contrast still names **this** form. | too_enough |
| **A13** | After past *said* / *told*, the complement is past (*she was tired*). No “no backshift” dodge. | say_tell 2026-09-03 |
| **C11** | Includes **sort**, not only Quiz. | have_to must vs have to |
| **C52** | Known pair gets its **own intro page** (applied here to grammar say/tell vs speak/talk). | say_tell 2026-09-03 |

Also live: C14 card 0 = unit name · C18 no slogan titles · B6 Quiz on-axis · B8 gap the chunk · B20 `sort_cz: false` on full-sentence sorts · E7 Use = error-correction · Type empty+Enter = no article when `zero_article` / `gap_answer: "—"`.

---

## Check / intro work already in the packs (do not redo)

- **`text_first: true`** on C1-fail card 0s rewritten this sitting.
- Form packs **Check = Quiz only**, authored chips: `comparatives` · `modals_must_should` · `first_conditional` · `prepositions_movement` · `quantifiers`.
- Tense timelines: keep diagram-first unless James asks (C29).
- Ignore a Match-duplicates post if it reappears — James said ignore.

Dirty on disk (do not revert): `a2_say_tell.json`, `a2_reflexives.json`, `a1_health.json`, `AUTHORING-RULES.md` (A13). Vocab C56 leftovers `a2_adverbs.json` / `a2_shopping.json` belong to the **vocab tab**.

---

## Telegram

Laptop listener: `TelegramSweepBot` on this machine. Ranking: `TA/smoke-order.json` (A2 circle only, hide ticked). Rebuild: `py -X utf8 codex\build_smoke_next.py --write`. After a tick: `py -X utf8 codex\reconcile_inspected.py`.

Tick: `a2_reflexives tested` then `a2_indirect_questions tested`.

If the phone still lists A1 vocab, the snapshot is stale — rebuild, then `units to test`. Id is `a2_…` not `as_…`.

Do not hand-edit `INSPECTED.md`. Reconcile may mention a phantom `questions_and_negatives` — ignore, do not invent that id.

---

## After a flag

I8: findings → **dropdowns** → rewrite. Not a drive-by. After Telegram `<id> tested`: new class → AUTHORING-RULES `observed` → reconcile → **new tab**.
