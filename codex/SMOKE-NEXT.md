# SMOKE NEXT — moved to the vault 2026-08-25

The running top-5 list lives at `Documents\original\TA\smoke-next.md` (Obsidian
Sync carries it to the home PC, where the Telegram bot reads it). Ticks land in
`TA/smoke-done-log.md` and are reconciled into INSPECTED.md here in session.

Bot contract (home PC): `TA/smoke-bot.md`. Full unit id; never rewrite the ranking;
`units to test` re-reads the file. This stub stays so repo readers know where
the list went.
