# Unit suggestions — dump during smoke

Do not invent node ids. A missing sniper → note it here → James asks → Codex row → then a pack.

Format:

```
YYYY-MM-DD  level  working title
  why (the error / the gap)
  neighbour (unit you were in)
```

---

## Parked from 2026-08-30 (James) — not units to build this sitting

- **Circle:** ~60 units per level, A1–C2 = 360. C2 is not in the app yet.
- **Level pass:** per level, Clozemaster-style cloze (pool Type sentences from units below) **and** a Use test. Above x% → fruit that level on the tree without each unit. Not today’s Exam Practice gym.
- **Spaces:** A1 −7 · A2 −18 · B1 −35 from 60. A2 already has 9 coming grammar ids. B1’s hole is mostly vocab (extension is a named program, not auto).
- **Level checks:** near the end of each level, easy → hard. Five units: vocab match · vocab type-in · grammar match-or-MC · grammar cloze · **one Use (vocab+grammar together)**. No separate “vocab Use”. Fruit-the-tree skip is shell, later. Pool from path units already taught. No ids this sitting.
- **A1 vocab check:** match sprint `a1_vocab_match` and type-in `a1_vocab_type` (last on A1 path, Topics, `#a1_vocab_type`). **No fruit** (Use only). Do next skips them so they cannot block the zigzag. Pool = live A1 vocab packs at runtime. Type-in drops frame sentences.
- **Level-check sequence:** `codex/LEVEL-CHECKS.md` — vocab match + type-in + grammar Which is correct? + grammar type-in + **A1 finale** (`a1_finale`, Use on everything, fruits this knot only).
- **A2 grammar coming (9):** ids already exist, no packs. Specs in `codex/A2-COMING.md`. First to write: `a2_articles`. Time 2 still has no node.

---

- **Relatives:** EGP A2 (8 statements); keep `b1_relative_clauses`. Known divergence, not a gap. Czech *the man which…* waits for B1.
- **or:** not in `a1_and_but_because` (and/but/because only; C11). Fold-in later, not a new A1 unit.
- **Claude extras (not in the nine, James will name which to add):** present continuous for arrangements; mine/yours; countries/nationalities; A2 phrasal pack; that-clauses / time clauses / infinitive of purpose / as…as / one / ones as fold-ins.
- **A1 slots (James 2026-08-30):** 59 live / 60. Vocab match + type + grammar Which + grammar type-in + **finale** already count. Spend left: **1 spare**. Countries leaf is live: `leaf_countries_a1` / pack `a1_countries`. Fold, don’t add nodes: mine/yours → possessives; very/really → adjectives trunk; or → and/but later; weather/transport thicken Nature/Places. Relatives stay B1.

---

## During vocab smoke
