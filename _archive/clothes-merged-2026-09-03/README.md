# leaf_clothes_a2 — retired 2026-09-03

7 words, holding a full slot on the locked 60-unit A2 circle. A2-PLAN.md budgeted
it at 13 and noted "Oxford A2 has no more garments"; it never filled. A1 teaches
Clothes as a full 23-word unit, so the topic is not lost.

Merged, nothing dropped:

| word | went to |
|------|---------|
| casual, formal | `a2_appearance` (33 -> 35), block "How they look" |
| clothing, uniform, pants, jumper, ring | `a2_shopping` (21 -> 26), new block "Clothes" |

Its 6 lemma-less recycle sentences (fashion, glove, jewellery, smart, belt, button)
were dropped: every one of those words is taught with its own sentence in
`a2_shopping`, `a2_appearance` or `a1_clothes`.

The freed circle slot went to `leaf_signals_a2` (Talking, signs & tools), which was
split out of the over-cap `a2_ideas` dump the same day. Spine step EN_A2_36 (Clothes,
vocab-only) was removed; `leaf_signals_a2` became the vocab half of EN_A2_28
(Verb patterns 1), where promise / suggestion / request pair with to- and -ing forms.
